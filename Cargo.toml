[package]
name = "rs-cross-spawn"
version = "0.1.3"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", features = ["napi4", "napi5"] }
napi-derive = "2"
cross-spawn = "0.1.3"

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
