[package]
name = "rs-cross-spawn"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", features = ["napi4", "napi5"] }
napi-derive = "2"
cross-spawn = "0.1"

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
